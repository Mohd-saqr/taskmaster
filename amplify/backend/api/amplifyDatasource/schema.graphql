

input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!

type Task @model {
  id: ID!
  title: String
  body:String
  state:String
}

type Team @model {
  id:ID!
  name: String
  tasks:[Task] @hasMany
}
